describe('Landing page detail component', () => {
  beforeEach(() => {
    console.log('Before Each')
  })
  
  beforeAll(() => {
    console.log('Before All')
  })


  afterEach(() => {
    console.log('After Each')
  })
  
  afterAll(() => {
    console.log('After All')
  })

  it.skip ('should test the landing page component', () => {
    expect(4 + 2 ).toEqual(6);
  })


  it ('should test testing the before guys', () => {
    expect(4 + 2 ).toEqual(6);
  })
  
  
});